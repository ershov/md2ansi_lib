# Project {#project}

## Metadata {#metadata}

    next_id: 88

## Tickets {#tickets}

* ## Ticket: Task: Implement md2ansi_lib.py per design doc {#1}

      status: done
      created: 2026-05-18 02:15:18 UTC
      updated: 2026-05-18 02:46:43 UTC

  Build the single-file zero-dependency Markdown→ANSI library described in `md2ansi_lib.design.md`. Output file: `md2ansi_lib.py` in this directory.

  Conventions (per design §3, §4):
  - Public names `M2A_*` / `md2ansi`; private names `_M2A_*` / `_m2a_*` / `_md2ansi`. No identifier escapes one of these prefixes.
  - Python 3.10+; f-strings, `match/case`, `@dataclass(slots=True)`. No `from __future__ import annotations`.
  - File layout follows design §9 — sections are added to the same file as the implementation grows.

  Final acceptance: `md2ansi_lib.md2ansi(text)` renders the full markdown surface in §7.1 and the design doc itself round-trips without exception.

  * ## Ticket: Phase 1: Module foundation {#2}

        status: done
        created: 2026-05-18 02:15:18 UTC
        updated: 2026-05-18 02:25:48 UTC

    Establish the file skeleton and all module-level constants that have no dependency on the dispatch engine.

    * ## Ticket: Task: Create `md2ansi_lib.py` skeleton {#3}

          status: done
          created: 2026-05-18 02:15:18 UTC
          updated: 2026-05-18 02:24:23 UTC

      Create the file with:
      - Module docstring (1-2 lines).
      - `import re` (only the foundation needs re; other imports added as later phases require them).
      - Numbered section comment markers per design §9 (`# Section 1` … `# Section 9`) so subsequent tickets drop code into the right slot.

      Verify: `python -c "import md2ansi_lib"` exits 0.

    * ## Ticket: Section 1: SGR color constants {#4}

          status: done
          created: 2026-05-18 02:15:18 UTC
          updated: 2026-05-18 02:24:56 UTC

      Define the bare SGR-code constants per design §7.3 (`M2A_COLOR_COMMENT`, `_STRING`, `_NUMBER`, `_KEYWORD`, `_BUILTIN`). Bare codes only — no `\e[...m` wrapping.

      Verify: from a Python REPL, `md2ansi_lib.M2A_COLOR_COMMENT == "38;5;245"`.

    * ## Ticket: Section 2: Dataclasses `M2A_Context` and `M2A_DocumentState` {#5}

          status: done
          created: 2026-05-18 02:15:18 UTC
          updated: 2026-05-18 02:25:22 UTC

      Per design §5.1:
      - `M2A_Context(compiled, rules)` — `@dataclass(frozen=True, slots=True)`.
      - `M2A_DocumentState(line_width=80, footnotes={}, footnote_order=[])` — `@dataclass(slots=True)` with `field(default_factory=...)` for the mutable defaults.

      Verify: instances construct and round-trip values; `M2A_Context` rejects attribute mutation.

    * ## Ticket: Section 3: Shared regex fragments {#6}

          status: done
          created: 2026-05-18 02:15:18 UTC
          updated: 2026-05-18 02:25:48 UTC

      Per design §6.2 and §7.2:
      - `_M2A_STR_DQ`, `_M2A_STR_SQ`, `_M2A_STR_BT`, `_M2A_STR_TDQ`, `_M2A_STR_TSQ`, `_M2A_NUM`.
      - `_M2A_BLOCK_START_AHEAD` (block-start lookahead used by inline rules' soft-newline branch).

      All fragments are raw strings designed to be embedded inside `re.VERBOSE` patterns. Add brief one-line comments referencing the design section; do not duplicate design rationale.

  * ## Ticket: Phase 2: Core regex engine {#7}

        status: done
        created: 2026-05-18 02:15:18 UTC
        updated: 2026-05-18 02:27:59 UTC

    Build the context-builder utility and substitution dispatcher so any well-formed rule table can be compiled and executed.

    * ## Ticket: Section 4: `_m2a_build_context` + placeholder rewrite {#8}

          status: done
          created: 2026-05-18 02:15:18 UTC
          updated: 2026-05-18 02:27:05 UTC

      Per design §5.3:
      - Define `_M2A_PLACEHOLDER_RE = re.compile(r"\(\?P([<=])\*(\w*)>")`.
      - Implement `_m2a_build_context(rules)`:
        1. For each rule rewrite `(?P<*name>...)` and `(?P=*name)` to `(?P<{rulename}_name>...)` / `(?P={rulename}_name)`. Empty suffix becomes `inner`.
        2. Wrap each rewritten pattern with `(?P<{rulename}>{rewritten})`.
        3. Join alternatives with `|`, compile under `re.VERBOSE | re.MULTILINE | re.DOTALL`.
        4. Return `M2A_Context(compiled=..., rules=tuple(rules))`.

      Verify on a one-rule fixture, e.g. `[("bold", r"\*(?P<*>[^*]+)\*", "1", None)]`: compiled regex matches `*x*`; `m.group("bold")` and `m.group("bold_inner")` resolve.

    * ## Ticket: Section 8: `_md2ansi` dispatcher {#9}

          status: done
          created: 2026-05-18 02:15:18 UTC
          updated: 2026-05-18 02:27:34 UTC

      Per design §5.4. Iterate `context.rules`; identify the firing alternative via the outer named group; then:
      - `str` fmt: emit `\x1b[{new_style}m{inner_or_match}\x1b[{current_style}m`, recursing into `recurse` if not None.
      - Callable fmt: call `func(m, current_style, context, state)` and return its result.

      Verify: a one-rule context that bolds `*foo*` produces `\x1b[0;1mfoo\x1b[0m` on input `*foo*`.

    * ## Ticket: Section 9: Minimal public `md2ansi` {#10}

          status: done
          created: 2026-05-18 02:15:18 UTC
          updated: 2026-05-18 02:27:59 UTC

      Per design §5.5 — initial form **without** footnote rendering. Build `state = M2A_DocumentState(line_width=line_width)`, return `_md2ansi(text, current_style, M2A_CONTEXT_MD, state)`. Defaults: `current_style="0"`, `line_width=80`.

      Note: `M2A_CONTEXT_MD` is a forward reference defined by Phase 5. This ticket only adds the function body; end-to-end verification is deferred to Phase 5. Until then verify dispatcher-only behavior via the previous ticket.

  * ## Ticket: Phase 3: Code-highlighting grammars {#11}

        status: done
        created: 2026-05-18 02:15:18 UTC
        updated: 2026-05-18 02:30:47 UTC

    Build the four code contexts. They're standalone (no dependency on the MD rule table) but the MD rule table will reference them via lambdas.

    * ## Ticket: Task: Python code rules + compiled context {#12}

          status: done
          created: 2026-05-18 02:15:18 UTC
          updated: 2026-05-18 02:30:47 UTC

      Per design §7.4. Define `_M2A_RULES_CODE_PYTHON` in the order listed (comments → triple-strings → strings → numbers → keywords → builtins). Keyword and builtin lists per the design's bullet lists, joined as `\b(?:kw1|kw2|...)\b`.

      Compile `M2A_CONTEXT_CODE_PYTHON = _m2a_build_context(_M2A_RULES_CODE_PYTHON)`.

      Verify: `_md2ansi("def f(): return 'x' + 42", "0", M2A_CONTEXT_CODE_PYTHON, M2A_DocumentState())` colors `def`, `return`, `'x'`, and `42`.

    * ## Ticket: Task: Bash code rules + compiled context {#13}

          status: done
          created: 2026-05-18 02:15:18 UTC
          updated: 2026-05-18 02:30:47 UTC

      Per design §7.5. Note `sh_comment` requires preceding `^` or whitespace so `$#`, `$?`, etc. aren't misread as comments. Keyword and builtin lists per the design table.

      Compile `M2A_CONTEXT_CODE_BASH`. Verify on `# comment\nif true; then echo hi; fi`.

    * ## Ticket: Task: JavaScript code rules + compiled context {#14}

          status: done
          created: 2026-05-18 02:15:18 UTC
          updated: 2026-05-18 02:30:47 UTC

      Per design §7.6. Both line and block comments (`//`, `/* ... */` with tempered-greedy). Template literals use backticks (`_M2A_STR_BT`).

      Compile `M2A_CONTEXT_CODE_JAVASCRIPT`. Verify on a small JS sample mixing strings, numbers, keywords, and both comment styles.

    * ## Ticket: Task: Generic (passthrough) code context {#15}

          status: done
          created: 2026-05-18 02:15:18 UTC
          updated: 2026-05-18 02:30:47 UTC

      Per design §7.7. Empty rule table: `M2A_CONTEXT_CODE_GENERIC = _m2a_build_context(())`. Compiled regex matches nothing; `_md2ansi` returns input unchanged. The codeblock wrapper styling is the job of `_m2a_fmt_code`, not this context.

      Verify: `_md2ansi("anything", "0", M2A_CONTEXT_CODE_GENERIC, state)` returns `"anything"` verbatim.

  * ## Ticket: Phase 4: Callable formatters (Section 5) {#16}

        status: done
        created: 2026-05-18 02:15:18 UTC
        updated: 2026-05-18 02:33:05 UTC

    Implement the callable handlers referenced from `_M2A_RULES_MD`. Each follows the signature in design §8: `(m, current_style, context, state) -> str`.

    * ## Ticket: Task: `_m2a_fmt_code` — fenced code-block dispatcher {#17}

          status: done
          created: 2026-05-18 02:15:18 UTC
          updated: 2026-05-18 02:33:05 UTC

      Per design §7.1 rows 8-11. Extract the code body from a fenced block (` ``` ` or `~~~`), run `_md2ansi(body, current_style, code_context, state)` on it, wrap in subtle codeblock styling (rely on the inner context's coloring; outer wrap = `current_style`). Accept an extra `code_context` argument bound by lambdas in `_M2A_RULES_MD`.

      Verify: a fenced ` ```python ` block produces ANSI-colored Python tokens inside.

    * ## Ticket: Simple formatters: `_m2a_fmt_hr`, `_m2a_fmt_inline_code`, `_m2a_fmt_image` {#18}

          status: done
          created: 2026-05-18 02:15:18 UTC
          updated: 2026-05-18 02:33:05 UTC

      - `_m2a_fmt_hr`: emits `─` × `(state.line_width - 1)` (design §10.2).
      - `_m2a_fmt_inline_code`: strips surrounding backticks, wraps body in code styling consistent with codeblocks.
      - `_m2a_fmt_image`: substitutes `![alt](url)` → `[IMG: alt]`, styled (e.g., italic + dim).

      Verify each via a small snippet.

    * ## Ticket: Task: `_m2a_fmt_blockquote` {#19}

          status: done
          created: 2026-05-18 02:15:18 UTC
          updated: 2026-05-18 02:33:05 UTC

      Per design §7.1 row 12. Match captures `> ...` lines. Render with a vertical-bar prefix (e.g., `│ `) and a dim style; recurse the inner content through `M2A_CONTEXT_MD` so nested inline formatting works.

      Verify: nested `**bold**` inside a quote renders bold; multi-line quotes preserve line structure.

    * ## Ticket: Task: `_m2a_fmt_table` {#20}

          status: done
          created: 2026-05-18 02:15:18 UTC
          updated: 2026-05-18 02:33:05 UTC

      Per design §7.1 row 13 and §10.2. Parse a pipe-table block:
      - Header row + separator row + body rows.
      - Column count from header; pad/truncate rows accordingly.
      - Column widths from max content length.
      - Render with box-drawing characters (top/middle/bottom borders, column separators).
      - Recurse cell content through `M2A_CONTEXT_MD` for inline formatting.

      Verify on a 2×3 table with inline `**bold**` in a cell.

    * ## Ticket: Task: `_m2a_fmt_list` {#21}

          status: done
          created: 2026-05-18 02:15:18 UTC
          updated: 2026-05-18 02:33:05 UTC

      Per design §7.1 row 14 + Notes. Indent-driven nesting (every 2 leading spaces = one level). Mixable markers (`-`, `*`, `+`, `\d+\.`). Render bullets as a styled marker (e.g., `•`); render ordered items with their original number. Recurse item text through `M2A_CONTEXT_MD`.

      Verify: a list with mixed bullets, an ordered sublist, and inline formatting renders correctly.

    * ## Ticket: Footnote handlers: `_m2a_fmt_footnote_def`, `_m2a_fmt_footnote_ref`, `_m2a_render_footnotes` {#22}

          status: done
          created: 2026-05-18 02:15:18 UTC
          updated: 2026-05-18 02:33:05 UTC

      Per design §10.3:
      - `_def`: store `id → text` (with continuation-line support per the multi-line pattern in §7.1 row 15) in `state.footnotes`; return `""` so nothing appears at the definition site.
      - `_ref`: append `id` to `state.footnote_order` if absent; render an inline styled `[^id]`.
      - `_render_footnotes(state, current_style)`: emit a `Footnotes:` heading + each referenced id in appearance order. Refs without definitions render `Missing footnote definition`.

      Verify on a doc with two refs and one definition: section appears with both entries; missing-def message is shown for the unmatched ref.

  * ## Ticket: Phase 5: Markdown grammar and wiring {#23}

        status: done
        created: 2026-05-18 02:15:18 UTC
        updated: 2026-05-18 02:38:25 UTC

    Wire the rule table, compile the MD context, and finalize the public entry point.

    * ## Ticket: Task: `_M2A_RULES_MD` + compiled `M2A_CONTEXT_MD` {#24}

          status: done
          created: 2026-05-18 02:15:18 UTC
          updated: 2026-05-18 02:38:24 UTC

      Per design §7.1, in the exact 25-rule order shown.
      - String fmt for heading / link / bold / italic / strike / underscore-variant rules.
      - Lambda wrappers around `_m2a_fmt_code` to bind the right code context for `code_python`, `code_bash`, `code_javascript`, `code_generic`.
      - Direct callable references for the remaining handlers.
      - Use `.format(_M2A_BLOCK_START_AHEAD=_M2A_BLOCK_START_AHEAD)` (or equivalent) for patterns containing the soft-newline branch.

      Order critical: bolditalic before bold before italic; image before link; code-* before blockquote/list/table.

      Then compile: `M2A_CONTEXT_MD = _m2a_build_context(_M2A_RULES_MD)`. Verify on a small mixed-content sample (heading + paragraph + fenced code + table + bold-italic) that ANSI escapes appear in expected positions.

    * ## Ticket: Task: Wire footnote rendering into `md2ansi` {#25}

          status: done
          created: 2026-05-18 02:15:18 UTC
          updated: 2026-05-18 02:38:25 UTC

      Finish design §5.5: after the `_md2ansi` call, if `state.footnote_order` is non-empty, concatenate `_m2a_render_footnotes(state, current_style)`.

      Verify: a doc with footnote refs and definitions ends with the Footnotes section in appearance order.

  * ## Ticket: Phase 6: End-to-end verification {#26}

        status: done
        created: 2026-05-18 02:15:18 UTC
        updated: 2026-05-18 02:46:42 UTC

    The design doc itself is a strong fixture — it exercises headings, lists, tables, fenced code, inline formatting, and minimal-intrusion behavior.

    * ## Ticket: Task: Targeted fixtures per block type {#27}

          status: done
          created: 2026-05-18 02:15:18 UTC
          updated: 2026-05-18 02:46:42 UTC

      Create `tests/test_md2ansi_lib.py` (pytest). One test per block type:
      - Headings h1-h6 emit expected color codes.
      - Bold / italic / strikethrough / bolditalic / underscore-variants.
      - Link, image, inline code.
      - Fenced code with Python, Bash, JS, generic.
      - Blockquote, table, list (mixed markers + nesting).
      - Footnote ref + def + missing-def.

      Run: `python -m pytest tests/test_md2ansi_lib.py -q`.

    * ## Ticket: Task: Render `md2ansi_lib.design.md` end-to-end {#28}

          status: done
          created: 2026-05-18 02:15:18 UTC
          updated: 2026-05-18 02:46:42 UTC

      From a small script: `print(md2ansi_lib.md2ansi(open("md2ansi_lib.design.md").read()))`. Verify:
      - No exceptions.
      - Output contains ANSI escape sequences (`\x1b[`).
      - Plain text and indentation are preserved per design §10.1.

      Capture a sample to `/tmp/design.ansi` for visual review.

  * ## Ticket: Task: Inline-only recurse context {#29}

        body: Block elements (headings, blockquote, table, list, links, bold/italic) currently recurse into the full MD context, so heading text like "1. Goals" becomes a list. Fix: introduce M2A_CONTEXT_MD_INLINE (inline rules only) and have block rules / callable handlers recurse into it instead.
        updated: 2026-05-18 02:46:42 UTC
        status: done
        created: 2026-05-18 02:44:47 UTC

* ## Ticket: Task: Add __main__: stdin or file args → stdout {#30}

      body: If no args, read stdin and render. If args, open each file in order and write the rendered output to stdout concatenated. Goes after Section 9 in md2ansi_lib.py — keep imports inside the __main__ guard so library users don\t pay for sys.
      updated: 2026-05-18 03:26:46 UTC
      status: done
      created: 2026-05-18 03:25:26 UTC

* ## Ticket: Task: Combine py_string rules with prefix support {#31}

      body: Replace py_string_tdq/tsq/dq/sq with a single py_string rule that allows valid Python prefixes (r/R/b/B/u/U/f/F, up to 2 chars). Anchor prefix at word boundary so it cannot bleed into a preceding identifier; allow empty prefix for unprefixed strings. Each fragment wrapped in its own (?:...) so internal | does not interfere with outer alternation.
      updated: 2026-05-18 03:46:27 UTC
      status: done
      created: 2026-05-18 03:44:36 UTC

* ## Ticket: Task: Table column alignment {#32}

      status: done
      created: 2026-05-21 05:03:09 UTC
      updated: 2026-05-21 05:30:00 UTC

  Parse the markdown separator row (e.g. `| :--- | ---: | :--: |`) to pick
  per-column alignment, applied uniformly to **header and body** cells.
  Default = left when the column has no marker, or when the table has no
  separator row at all.

  Implementation notes:
  - Add a tiny `_m2a_align_cell(content, width, align)` helper that distributes
    padding for left / right / center using `_m2a_visible_len`.
  - In `_m2a_fmt_table`, after detecting the separator row, build a per-column
    `aligns` list (`"left" | "right" | "center"`).
  - Render every row (header + body) with `aligns[i]`.

  Tests:
  - Each marker (`:---`, `---:`, `:---:`) drives correct padding distribution
    in both header and body cells.
  - Mixed alignment row (one column each).
  - No separator row → all cells left.

  * ## Comments {#32:comments}

    * Implemented: _m2a_align_cell helper + per-column align list parsed from separator row in _m2a_fmt_table; 5 new tests. {#32:comment:35}

* ## Ticket: Task: Table cell wrapping when row exceeds line_width {#33}

      status: done
      created: 2026-05-21 05:03:09 UTC
      updated: 2026-05-21 05:36:33 UTC

  Reduce the table width to fit `state.line_width` (when > 0) by wrapping
  oversized cells.

  Layout algorithm:
    fixed   = columns whose natural width ≤ cell_min_width (never shrunk)
    fit_w   = line_width − overhead, where overhead = 3*N + 1
              (each cell adds 2 padding chars + 1 right rail; +1 left rail)
              fit_w -= sum(W[i] for i in fixed)
    wide    = [i not in fixed]
    if sum(W[wide]) <= fit_w: stop  # already fits
    loop:
      factor = fit_w / sum(W[wide])
      progressed = False
      for i in wide:
        new = int(W[i] * factor)
        if new <= cell_min_width:
          W[i] = cell_min_width; move i from wide -> fixed; progressed = True
        else:
          W[i] = new
      if progressed: re-enter loop (re-compute fit_w against the new fixed set)
      else: stop
    if total still overflows after all cells are pinned at cell_min_width:
      accept the overflow (per spec).

  Rendering (initial implementation):
  - Wrap **raw** cell text first with `_m2a_wrap_line(text, col_width, "")`,
    then re-render each wrapped sub-line through
    `_md2ansi(sub, current_style, M2A_CONTEXT_MD_INLINE, state)`.
    Add a code comment flagging the known caveat: any inline rule whose
    rendered width differs from raw text width (links `[x](url)` lose the
    URL; images `![alt](url)` become `[IMG: alt]`) will mis-account width.
    Tracked separately under [[fix-table-wrap-after-render]].
  - Pad each rendered sub-line to the column width via the alignment helper
    from [[table-column-alignment]] (left/right/center per column).
  - Row height = max(len(cell_lines[i])) over the row's cells.
    Top-align: shorter cells get trailing blank-line padding so all cells
    in a row finish at the same visual line.
  - Build the row by joining per-column sub-lines with `│`, one output line
    per sub-line.

  Options on `md2ansi(...)`:
  - `cell_min_width=20` — columns whose natural width is below this are
    never shrunk and never wrapped.
  - `row_dividers=None` — tristate:
      - `None` (default): auto. Emit `├─...─┼─...─┤` between body rows
        only if **any** body row produced multi-line cells.
      - `True`: always emit row dividers.
      - `False`: never emit row dividers.

  Non-goals / notes:
  - Cells are split on `|` before `_md2ansi` ever sees them, so inline
    rules can't bleed across cells without extra guarding.
  - If `line_width == 0`, skip the layout step entirely — render at natural
    widths (existing behavior).

  Tests:
  - Wide column shrinks; short column untouched.
  - Iterative pass: wide cell that drops below `cell_min_width` is pinned;
    remaining wide cells re-scaled.
  - Multi-line cell renders with top-aligned blank-line padding.
  - `row_dividers=True` forces dividers; `False` suppresses them.
  - `row_dividers=None` auto-enables dividers when any cell wraps; auto-omits
    them when no cell wraps.
  - All-cells-narrow table renders unchanged regardless of `line_width`.
  - Cell with `**bold**` stays styled across wrapped sub-lines.

  * ## Comments {#33:comments}

    * Implemented shrink-to-fit table layout with iterative column rescaling, raw-then-render wrap, top-aligned multi-line cells, and tristate row_dividers. Added two M2A_DocumentState fields (cell_min_width, row_dividers) plus table_fit_width so the 150-char HR fallback never triggers shrinking. Deviation from spec: after wrapping, the per-column width is bumped up to the widest actual sub-line, since _m2a_wrap_line can leave single long words unsplit and they would otherwise overflow the assigned column. 9 new tests; all 84 pass. {#33:comment:36}

* ## Ticket: Task: Table cell wrap: account for width-changing inline rules {#34}

      body: The initial table-cell-wrap (ticket #33) wraps **raw** cell text and then renders each wrapped sub-line through _md2ansi. This is wrong whenever an inline rule changes the visible width relative to the raw text:
      status: done
      updated: 2026-05-21 06:14:24 UTC

  - Links: ``[label](url)`` raw width is len(label) + len(url) + 4, rendered width is just len(label).
  - Images: ``![alt](url)`` raw width is len(alt) + len(url) + 5, rendered width is len(``[IMG: alt]``).

Result: a cell containing a link or image gets wrapped to a column that is wider than its actual rendered content, or (less commonly) narrower. The column-width math is off by the inline-rule shrinkage.

Fix approaches:
  - Render first, then wrap with a visible-length-aware variant of _m2a_wrap_line that treats ANSI escape sequences as zero-width. Requires teaching _m2a_wrap_line about \\x1b[...m escapes.
  - Or: keep raw-text wrapping but pre-substitute the would-be rendered string for each inline rule so width accounting matches reality.

Pick once a real workload surfaces a visible bug.
      updated: 2026-05-21 05:19:51 UTC
      status: open
      created: 2026-05-21 05:19:51 UTC

  * ## Comments {#34:comments}

    * Closed by implementing _m2a_wrap_ansi_line: tokenizes the rendered cell into ANSI-escape / whitespace / word runs, wraps at visible-character positions, re-emits the last seen SGR after every break so styling carries across wrapped lines. _m2a_fmt_table now passes the rendered cell (not raw text) to the wrap helper. Verified: bold and inline-code spans survive the wrap; literal ** and ` no longer leak into output. {#34:comment:37}

* ## Ticket: Epic: Structural scan API + frontmatter support {#38}

      status: done
      created: 2026-06-01 04:29:59 UTC
      updated: 2026-06-01 05:02:52 UTC

  Add `md2ansi_scan` (structural span generator) and frontmatter rendering to `md2ansi_lib.py`.
  Design spec: `docs/superpowers/specs/2026-06-01-md2ansi-structural-scan-design.md`.

  Acceptance:
  - `md2ansi_scan(text, kinds=M2A_SPANS_BLOCK)` yields `M2A_Span` records in document order, filtered by kind; raises `ValueError` on unknown kinds.
  - Frontmatter renders as a framed "Frontmatter" box and appears as `kind="frontmatter"` in scans.
  - Existing test suite stays green; new tests cover scan + frontmatter.
  - Out of scope (separate follow-up): re-vendoring to `recipes/md2ansi.py`, the `browse-md` `_parse` collapse, `recursive=` scanning.

  * ## Comments {#38:comments}

    * Scan API + frontmatter complete: md2ansi_scan yields M2A_Span filtered by kind; frontmatter renders as framed box & appears as kind=frontmatter. 118 tests pass. Out of scope (separate follow-up): re-vendor to recipes/md2ansi.py, browse-md _parse collapse, recursive= scanning. {#38:comment:45}

  * ## Ticket: Task: Frontmatter block rule + framed rendering {#39}

        status: done
        created: 2026-06-01 04:29:59 UTC
        updated: 2026-06-01 04:43:10 UTC

    Add `_MD_FRONTMATTER` (`\A`-anchored, empty `(?P<*indent>)`, tempered-greedy `(?P<*body>)`, closing `---`), placed FIRST in `_M2A_RULES_MD` (must precede `hr`). Add optional `label` param to `_m2a_fmt_code` (default keeps "Code[: lang]"); bind frontmatter via lambda to `M2A_CONTEXT_CODE_GENERIC` with `label="Frontmatter"`. Teach `_m2a_wrap_source` to skip a leading frontmatter block (like it skips code fences).
    TDD tests: `\A---\n...\n---` -> framed box, body verbatim (not md-parsed); lone `---` still HR; mid-document `---` still HR; long YAML line not wrapped under `line_width>0`.
    Docs: add the frontmatter bullet to `md2ansi_lib.design.md` §10.2 (intrinsic exceptions to minimal intrusion).

    * ## Comments {#39:comments}

      * Implemented frontmatter rule + framed 'Frontmatter' rendering. Strict body (no blank lines / no # comments) so real markdown isn't mis-matched; closing --- ends at $ (no newline consumed) so the box doesn't merge with following content; _m2a_wrap_source skips the leading frontmatter block (mirrors the rule). 8 new tests; 106 total pass. {#39:comment:42}

  * ## Ticket: Task: M2A_Span data model + kind mapping + kind-set constants {#40}

        status: done
        created: 2026-06-01 04:29:59 UTC
        updated: 2026-06-01 05:00:14 UTC

    Add `@dataclass(frozen=True, slots=True) M2A_Span(kind, subtype, is_block, start, end, text)`. Add a small static kind/subtype map (headings -> heading/h1..h6; code_python/bash/js -> code/code-python|code-bash|code-javascript; emphasis group -> emphasis/bold|italic|bolditalic|strike); fallback `kind=subtype=rule name`; `is_block` = rule name not in `_M2A_RULES_INLINE_RAW`; `code_generic` subtype from fence tag (`code-<tag>` or `code`). Add `M2A_SPANS_BLOCK` / `M2A_SPANS_INLINE` / `M2A_SPANS_ALL` frozensets, derived from the rule tables.

    * ## Comments {#40:comments}

      * Added M2A_Span(kind, subtype, is_block, start, end, text) frozen dataclass, _M2A_SPAN_KINDS mapping (heading/code/emphasis grouped; code- namespaced subtypes; fallback kind==subtype==rule name), and M2A_SPANS_BLOCK/INLINE/ALL derived from the rule tables. 4 tests. {#40:comment:43}

  * ## Ticket: Task: md2ansi_scan generator + kind validation {#41}

        status: done
        created: 2026-06-01 04:29:59 UTC
        updated: 2026-06-01 05:02:52 UTC

    Add `def md2ansi_scan(text, kinds=M2A_SPANS_BLOCK)`: validate `set(kinds) <= M2A_SPANS_ALL` (raise `ValueError` listing unknowns); `M2A_CONTEXT_MD.compiled.finditer(text)`; identify outer rule via first non-None group (as `_m2a_replace`); map to `(kind, subtype, is_block)`; filter by `kind in kinds`; yield `M2A_Span`. Non-recursive.
    TDD tests: document order; `text[start:end]` round-trip; default block-only (no inline leak); `{'heading','list'}` whitelist; `M2A_SPANS_ALL` surfaces top-level inline; unknown kind raises `ValueError`; code subtype namespacing; emphasis grouping; frontmatter span present.

    * ## Comments {#41:comments}

      * Implemented md2ansi_scan(text, kinds=M2A_SPANS_BLOCK) + _m2a_scan workhorse. Eager kind validation (ValueError); finditer over M2A_CONTEXT_MD; first-non-None-outer-group rule id (as _m2a_replace); code_generic subtype from fence tag. 8 tests; offsets round-trip (incl Unicode). 118 total pass. {#41:comment:44}

* ## Ticket: Task: Tighten # in _BSA to require a space after hashes {#46}

      updated: 2026-06-08 06:52:59 UTC
      status: done
      created: 2026-06-08 06:51:10 UTC

  * ## Comments {#46:comments}

    * Changed \# -> \#{1,6}[ \t] in _M2A_BLOCK_START_AHEAD so a bare #word no longer stops a cross-line inline span; added 2 tests. Full suite 122 passing. {#46:comment:47}

* ## Ticket: Task: Redesign line wrapping: wrap after ANSI applied (post-render) {#48}

      updated: 2026-06-08 07:10:55 UTC
      status: done
      created: 2026-06-08 06:58:00 UTC

  * ## Ticket: Task: RED: failing tests for inline-span-across-wrap and list/blockquote wrap {#49}

        updated: 2026-06-08 07:04:34 UTC
        status: done
        created: 2026-06-08 06:58:09 UTC

  * ## Ticket: Task: GREEN: post-render ANSI-aware wrapping (delete source-wrap) {#50}

        updated: 2026-06-08 07:09:40 UTC
        status: done
        created: 2026-06-08 06:58:10 UTC

  * ## Ticket: Task: Wire formatters: blockquote/list self-wrap; code blocks opaque {#51}

        updated: 2026-06-08 07:09:40 UTC
        status: done
        created: 2026-06-08 06:58:10 UTC

  * ## Ticket: Task: Verify full suite + render q and design doc end-to-end {#52}

        updated: 2026-06-08 07:10:55 UTC
        status: done
        created: 2026-06-08 06:58:10 UTC

    * ## Comments {#52:comments}

      * Verified: q reproducer clean at all widths (no literal **/backtick, no NUL leak, widths respected); design.md renders end-to-end; scan + CLI smoke pass; full suite 126 passed. {#52:comment:53}

* ## Ticket: Code highlighting: C/C++, universal punctuation dimming, unknown-block coloring {#54}

      status: done
      created: 2026-06-09 02:15:30 UTC
      updated: 2026-06-09 02:24:39 UTC

  Three additions to md2ansi_lib.py syntax highlighting. Design agreed with user.

  Mechanism recap: each language is a rules tuple (name, regex, color, recurse)
  combined first-match-wins by `_m2a_build_context`; matched spans colored,
  unmatched text passes through. Punctuation color reuses gray 38;5;245.
  TDD throughout: write failing tests first, then implement.

  * ## Comments {#54:comments}

    * All subtasks complete. Three features shipped: C/C++ grammar, universal punctuation dimming (38;5;245), unknown/unmarked block coloring (multiline-permissive strings + numbers + punct). Frontmatter kept as passthrough. {#54:comment:67}

  * ## Ticket: Foundation: punct color, punct rule, permissive multiline string fragments {#55}

        status: done
        created: 2026-06-09 02:15:30 UTC
        updated: 2026-06-09 02:18:08 UTC

    No behavior change — just shared definitions used by later tickets.
    - Add `M2A_COLOR_PUNCT = "38;5;245"` near the universal palette (top of file).
    - Add `_M2A_PUNCT` VERBOSE regex fragment matching a run of punctuation:
      `[-+*/%=<>!&|^~.,;:?@(){}\[\]]+` (no backslash, no underscore, no quotes/backtick —
      those are handled by string/escape rules; quotes left out so a stray quote in an
      unknown block isn't dimmed mid-string).
    - Add a shared rule entry `_M2A_RULE_PUNCT = ("punct", _M2A_PUNCT, M2A_COLOR_PUNCT, None)`
      to be appended LAST to every code ruleset.
    - Add permissive multiline string fragments for the unknown-block context:
      `_M2A_STR_DQ_ML`, `_M2A_STR_SQ_ML`, `_M2A_STR_BT_ML` — same shape as the existing
      `_M2A_STR_*` but WITHOUT the `\n` exclusion in the negated class, so strings may
      span linebreaks (user accepts the over-match tradeoff).

    * ## Comments {#55:comments}

      * Added M2A_COLOR_PUNCT=38;5;245, _M2A_PUNCT fragment, _M2A_RULE_PUNCT shared rule; appended to Python ruleset. Full suite green (127). {#55:comment:61}

  * ## Ticket: Task: C/C++ ruleset + context + fence binding (TDD) {#56}

        status: done
        created: 2026-06-09 02:15:30 UTC
        updated: 2026-06-09 02:21:38 UTC

    Single shared ruleset for both languages (simplicity over precision).
    Tests first in tests/test_md2ansi_lib.py: a ```c block and a ```cpp block —
    assert keyword (e.g. `int`/`class`), comment (`//`, `/* */`), string, number,
    and preprocessor (`#include`) get their expected SGR; assert frame label.
    Implement `_M2A_RULES_CODE_C`:
    - preprocessor: `^[ \t]*\#[ \t]*\w+` -> M2A_COLOR_KEYWORD (before comments; C has no # comment)
    - line comment `//[^\n]*` and block comment `/\*...\*/` (reuse JS patterns) -> COMMENT
    - strings: `_M2A_STR_DQ` and char literal `_M2A_STR_SQ` -> STRING
    - number `_M2A_NUM` -> NUMBER
    - keywords (C + C++ control words AND fundamental types) -> KEYWORD
    - builtins (stdlib: printf, malloc, free, std, cout, cin, vector, string, map ...) -> BUILTIN
    - append `_M2A_RULE_PUNCT`
    Build `M2A_CONTEXT_CODE_C`. Add `_MD_CODE_C = _fenced(r"(?:c|cpp|c\+\+|cc|cxx|h|hpp|hxx)")`
    and a `code_c` rule in `_M2A_RULES_MD` BEFORE `code_generic`, label "C/C++".
    Also extend the block-kind subtype map (`_M2A_BLOCK_SUBTYPES` near line 1128) with
    `"code_c": ("code", "code-c")` so md2ansi_scan tags it.

    * ## Comments {#56:comments}

      * Added shared C/C++ ruleset (_M2A_RULES_CODE_C), M2A_CONTEXT_CODE_C, _MD_CODE_C fence (c/cpp/c++/cc/cxx/h/hpp/hxx), code_c rule before code_generic, code-c subtype. Full suite green (137). {#56:comment:63}

  * ## Ticket: Task: Universal punctuation dimming for Python/Bash/JS (TDD) {#57}

        status: done
        created: 2026-06-09 02:15:30 UTC
        updated: 2026-06-09 02:19:17 UTC

    Append `_M2A_RULE_PUNCT` to `_M2A_RULES_CODE_PYTHON`, `_M2A_RULES_CODE_BASH`,
    `_M2A_RULES_CODE_JAVASCRIPT` (last position, after keywords/builtins so it never
    steals `.` in a float or `/` in `//`). Tests: in a python block, assert a `.` or
    `(` carries the punct SGR while `def` still carries keyword SGR.

    * ## Comments {#57:comments}

      * Appended _M2A_RULE_PUNCT to bash and JS rulesets. Guard tests confirm punct never steals float dots or comment slashes. Full suite green (131). {#57:comment:62}

  * ## Ticket: Task: Unknown/unmarked block coloring (TDD) {#58}

        status: done
        created: 2026-06-09 02:15:30 UTC
        updated: 2026-06-09 02:23:26 UTC

    New `_M2A_RULES_CODE_UNKNOWN` = permissive multiline strings + `_M2A_NUM` + `_M2A_RULE_PUNCT`
    (NO comment rule — comment syntax is unknown). Build `M2A_CONTEXT_CODE_UNKNOWN`.
    Rebind the `code_generic` rule in `_M2A_RULES_MD` to use it (was CODE_GENERIC).
    IMPORTANT: leave `frontmatter` on the no-rule `M2A_CONTEXT_CODE_GENERIC` so YAML
    frontmatter stays a true passthrough. Tests: a ``` block with no tag and one with an
    unknown tag (e.g. ```rust) get numbers/strings/punct colored; a multiline string spans
    linebreaks; frontmatter still passes through verbatim.

    * ## Comments {#58:comments}

      * Added permissive multiline string fragments (_M2A_STR_DQ_ML/_SQ_ML), _M2A_RULES_CODE_UNKNOWN (strings+number+punct, no comments), M2A_CONTEXT_CODE_UNKNOWN; rebound code_generic to it. Frontmatter stays on CODE_GENERIC passthrough (guarded by test). Only test_fenced_code_generic_passes_through now fails (expected, #59). {#58:comment:64}

  * ## Ticket: Task: Update existing tests broken by new coloring {#59}

        status: done
        created: 2026-06-09 02:15:30 UTC
        updated: 2026-06-09 02:23:52 UTC

    `test_fenced_code_generic_passes_through` (line ~209) asserts `**not bold**` survives
    verbatim in an untagged block — punctuation dimming now injects SGR between the `*`s.
    Rewrite it to assert against strip_ansi output instead, or assert the punct SGR is
    present. Audit other Python/Bash/JS colorization tests for raw-substring assertions on
    punctuation. Frontmatter tests should remain green (separate passthrough context).

    * ## Comments {#59:comments}

      * Rewrote test_fenced_code_generic_passes_through -> test_fenced_code_generic_no_markdown_parsing: asserts ** survives in stripped output and no bold SGR, preserving original intent. Full suite green (143). {#59:comment:65}

  * ## Ticket: Task: Full verification + visual sanity {#60}

        status: done
        created: 2026-06-09 02:15:30 UTC
        updated: 2026-06-09 02:24:39 UTC

    Run the complete pytest suite (must pass). Render a small fixture covering c/cpp,
    an untagged block, and a python block through md2ansi and eyeball the ANSI so the
    user can confirm the 38;5;245 punctuation gray reads well.

    * ## Comments {#60:comments}

      * Full suite 143 passed. Smoke-rendered md2ansi_lib.design.md (exit 0, no traceback). Import clean under -W error. Visual render confirms c/cpp/python/unknown/rust coloring and 38;5;245 punctuation gray. {#60:comment:66}

* ## Ticket: Task: Code-span-aware table cell splitting {#68}

      updated: 2026-06-09 07:13:17 UTC
      status: done
      created: 2026-06-09 07:08:47 UTC

  Make _m2a_split_table_row treat inline code spans as opaque so an un-escaped | inside backticks is NOT a column divider.

  Change: extend _M2A_TABLE_CELL_RE's cell-content alternation to try a code-span branch BEFORE the [^|\\n] catch-all. Cover both double-backtick (``...``) and single-backtick (`...`, honoring \` escape) spans, mirroring _MD_CODE_INLINE2 / _MD_CODE_INLINE.

  Constraints:
  - Keep the position-walking splitter (re.match at pos); only extend the per-cell content pattern. Bare-char branch stays [^|\\n] so | is only ever a divider.
  - Stay linear / tempered-greedy. No backtracking across the row.
  - Keep honoring \| everywhere (existing behavior, leave as-is).
  - Scope to code spans only — NOT all inline rules (bold/links must not hide pipes).

  Tests (in tests/test_md2ansi_lib.py):
  - | aaa | `ccc | ddd` |  -> ['aaa', '`ccc | ddd`']  (the bug)
  - double-backtick span with a pipe stays one cell
  - unbalanced backtick: | a `b | c |  degrades to today's behavior (| still splits)
  - delimiter/alignment row |:--|--:| unaffected
  - existing \| escape still honored
  - a render-level (_md2ansi/md2ansi) assertion that the pipe shows inside the code span

  Out of scope: plain-cell \|->| unescaping (orthogonal, separate ticket if wanted).

  * ## Comments {#68:comments}

    * Done. Extended _M2A_TABLE_CELL_RE with double- then single-backtick code-span branches ahead of the catch-all; escape (\.) stays first so \` doesn't open a span, and backslash semantics are byte-for-byte unchanged (catch-all still [^|\\n]). Code-only scope as agreed — bold/links do not hide pipes. {#68:comment:69}

      Tests added in tests/test_md2ansi_lib.py (new 'Code-span-aware cell splitting' section): the motivating bug, double-backtick span, end-to-end render (pipe inside the styled run), plus regression guards for unbalanced backtick (degrades, | still splits), \| escape, and the delimiter row.
      Verified: 149 passed (was 142). Linearity holds — 50k-char unclosed-backtick row splits in ~10ms, no catastrophic backtracking. Not committed (left for review).

* ## Ticket: Task: Fix linked-image rendering: [![alt](img)](url) leaks raw markdown {#70}

      updated: 2026-06-11 00:12:38 UTC
      status: done
      created: 2026-06-11 00:11:11 UTC

  * ## Comments {#70:comments}

    * Root cause: link-text class [^\]\n\\] stopped at the image's closing ], so the link matched [![alt](img) and left ](url) as literal text. Fix: added non-capturing _MD_IMAGE_INLINE as the first alternative in the link-text group so a linked image is consumed whole. Added test_linked_image_renders_image_label. 150 tests pass. {#70:comment:71}

* ## Ticket: Task: Render headings nested in list items and blockquotes {#72}

      updated: 2026-06-17 15:03:08 UTC
      status: done
      created: 2026-06-17 15:00:30 UTC

  * ## Ticket: Task: Block-lite context + wire list/blockquote recursion (with comments) {#73}

        updated: 2026-06-17 15:02:25 UTC
        status: done
        created: 2026-06-17 15:00:30 UTC

  * ## Ticket: Task: Tests for covered nested-heading cases {#74}

        updated: 2026-06-17 15:03:08 UTC
        status: done
        created: 2026-06-17 15:00:30 UTC

* ## Ticket: Epic: Flat HTML tags & entities {#75}

      status: open
      created: 2026-06-24 01:12:42 UTC
      updated: 2026-06-24 01:12:42 UTC

  Implement flat (non-nesting) HTML support in `md2ansi_lib.py` per the approved spec
  `docs/superpowers/specs/2026-06-24-md2ansi-html-flat-tags-design.md`: comments (drop),
  `<br>`, `<hr>` (block + in-container), and entities. Zero engine changes — additive
  rule tuples + small handler edits only.

  Standing rules for every subticket (enforced by the dispatch prompt):
  - TDD: write failing tests first (in `tests/test_md2ansi_lib.py`), then implement.
  - Run the full suite (`python -m pytest tests/ -q`) green before finishing.
  - Touch only rule tables + handlers; do NOT modify `_md2ansi`, `_m2a_build_context`,
    the placeholder rewrite, or the style stack.
  - Each ticket is ONE commit on `main` (amend if review feedback lands).
  - Follow the namespacing conventions (`M2A_*`/`_M2A_*`/`_m2a_*`).

  * ## Ticket: Task: HTML comments — `<!-- ... -->` drop {#76}

        status: done
        created: 2026-06-24 01:12:42 UTC
        updated: 2026-06-24 01:18:40 UTC

    Spec §5.1. Add `html_comment` to `_M2A_RULES_INLINE_RAW` (immediately after the
    `escape` rule), pattern `<!-- (?: (?! --> ) [\s\S] )* -->` (tempered-greedy,
    multi-line), handler returns `""`. Add robustness: in `_m2a_fmt_table`, strip
    comments from each raw row line BEFORE `_m2a_split_table_row`. Add scan kind
    `html_comment -> ("comment","comment")` in `_M2A_SPAN_KINDS`.

    Tests: dropped in prose / heading / list item / blockquote / table cell; preserved
    literal inside a fenced code block and an inline code span; multi-line top-level
    comment drops wholesale; a comment containing `|` on a table row does NOT add
    columns; unclosed `<!--` passes through literally.

    * ## Comments {#76:comments}

      * Implemented HTML comment drop (spec 5.1). Added _MD_HTML_COMMENT pattern (tempered-greedy, multi-line) + compiled twin _M2A_HTML_COMMENT_RE; _m2a_fmt_comment handler (returns ""); html_comment rule in _M2A_RULES_INLINE_RAW immediately after escape; comment-strip in _m2a_fmt_table before _m2a_split_table_row; html_comment -> (comment,comment) in _M2A_SPAN_KINDS. Verified rather than assumed: code spans/blocks keep the literal because code_inline* precede the rule in the alternation and code contexts carry no comment rule; escaped \<!-- stays literal because escape fires first. Edge cases checked: multi-line top-level drop wholesale, comment with | does not add table columns (incl. whole-comment row), unclosed <!-- passes through, link-text comment dropped. Updated existing exhaustive test_span_inline_kinds_contents to include comment. 11 new tests + 1 updated; full suite 168 passed (baseline 157). {#76:comment:82}

  * ## Ticket: Task: Block-level `<hr>` — standalone full-width {#77}

        status: done
        created: 2026-06-24 01:12:42 UTC
        updated: 2026-06-24 01:28:05 UTC

    Spec §5.3 (block rule only). Add `html_hr` block rule to `_M2A_RULES_MD` near the
    existing `hr` rule: `^ [ \t]* (?i: < hr [ \t]* /? > ) [ \t]* $`, reusing the
    existing `_m2a_fmt_hr` handler. Add scan kind `html_hr -> ("hr","hr")`.

    Tests: standalone `<hr>` line → full-width rule; with leading whitespace; case and
    self-closing variants (`<HR>`, `<hr/>`, `<hr />`); a line with text after `<hr>` is
    NOT treated as a block rule. (In-container `<hr>` is a later ticket.)

    * ## Comments {#77:comments}

      * Added _MD_HTML_HR pattern source (^[ \t]*(?i:<hr[ \t]*/?>)[ \t]*$) near _MD_HR; added html_hr block rule to _M2A_RULES_MD immediately after the existing hr rule, reusing _m2a_fmt_hr directly (full-width bar from state.line_width, recurse None); added scan kind html_hr->('hr','hr') in _M2A_SPAN_KINDS. Placement is unshadowed: frontmatter/hr need ---/===/___, so a standalone <hr> wins html_hr. M2A_SPANS_BLOCK and is_block auto-derive (hr already a block kind). Tests (7, written first): standalone <hr> full-width (=line_width-1, mirrors markdown-HR test); bar tracks line_width at width 25; leading whitespace; case-insensitive <HR>; self-closing <hr/> and <hr />; '<hr> x' is NOT a block bar. No sentinels / engine changes. Suite 175 passed (168 baseline + 7). {#77:comment:83}

  * ## Ticket: Task: Sentinel infrastructure + input sanitizer {#78}

        status: done
        created: 2026-06-24 01:12:42 UTC
        updated: 2026-06-24 01:35:11 UTC

    Spec §2, §4, §6. Foundation for deferred layout constructs — NO producers yet.
    - Add constants `_M2A_LINEBREAK="\x01"`, `_M2A_RULE="\x02"`, `_M2A_NBSP="\x03"`.
    - Rewrite the input step in `md2ansi()`: normalize `\r\n`/`\r` → `\n`, then replace
      `[\x00-\x08\x0B\x0C\x0E-\x1A\x1C-\x1F]` with `�` (U+FFFD). This keeps `\t`,`\n`,
      `\x1b`, and neutralizes any stray sentinel in the source.
    - Extend the final-pass sweep `_m2a_wrap_rendered` to realize residual sentinels:
      non-opaque prose split on `\x01` (each piece wrapped, continuation = leading ws);
      `\x02` (uncontained) → a `─ × line_width` line of its own; `\x03` → `" "` on BOTH
      opaque and non-opaque lines.

    Tests (unit-level, since no producer yet): raw control chars in source → `�`
    (`\x00`,`\x07`,`\x1f`), while `\t`/`\n` survive and `\r\n`→`\n`; a stray `\x01`/`\x03`
    in source → `�`; call `_m2a_wrap_rendered` directly on strings containing `\x01`
    (prose split), `\x03` (→space), `\x02` (→full-width line) and assert realization.

    * ## Comments {#78:comments}

      * Implemented sentinel infrastructure + input sanitizer (no producers). {#78:comment:84}

        Constants (after _M2A_OPAQUE): _M2A_LINEBREAK='\x01', _M2A_RULE='\x02', _M2A_NBSP='\x03', each commented with its deferred-layout meaning. Also added module-level compiled _M2A_C0_KILL = re.compile(r'[\x00-\x08\x0B\x0C\x0E-\x1A\x1C-\x1F]') (re is imported at line 8, well before).
        Input sanitizer in md2ansi(): replaced the old text.replace(_M2A_OPAQUE,'') with: normalize \r\n then lone \r to \n, then _M2A_C0_KILL.sub('\ufffd', text). Keeps \t/\n/ESC(\x1b); subsumes the old NUL strip; neutralizes stray \x00-\x03 in SOURCE before render so emitted sentinels stay unambiguous.
        Final-pass realization in _m2a_wrap_rendered(): computes rule_line once = '─'*max(1,W-1) with W=line_width if >0 else 150 (mirrors _m2a_fmt_hr; plain/uncolored bar per ticket for the uncontained-prose edge). Opaque branch unchanged except the global trailing .replace(_M2A_NBSP,' ') now also realizes \x03 on opaque lines. Non-opaque: split on \x01 (each piece its own block), then each piece split on \x02 (each boundary emits rule_line on its own line; empty segment adjacent to a rule is skipped so a bare \x02 -> exactly one rule line, no blank prose lines); prose segs wrapped via _m2a_wrap_ansi_line when line_width>0 else appended verbatim. \x03 -> ' ' applied LAST over the joined output (after wrapping) so nbsp stays glued (outside the whitespace class) through wrapping, then becomes a space.
        Decision: on opaque lines I realize ONLY \x03, not \x01/\x02 — an opaque line owns its single-physical-line layout and marker-per-line contract; splitting it or inserting a multi-line rule would violate the opaque invariant. Ticket allowed leaving \x01/\x02 to container handlers (later ticket); in practice headings convert them before marking opaque.
        Common (no-sentinel) path is behaviorally identical: ln.split('\x01')->[ln], .split('\x02')->[ln], len==1 so guard skipped, wrap exactly as before. Verified by 175 baseline tests unchanged + a manual smoke render (blank lines, heading, block ---, list all intact, zero sentinel leak).
        Tests (TDD, written first, 15 new): constants distinct/correct; sanitizer maps \x00/\x07/\x1f to U+FFFD, keeps \t/\n, normalizes \r\n and lone \r, keeps a full ESC SGR seq, neutralizes stray \x01/\x02/\x03 in source to U+FFFD (not realized), stray \x02 in source is not a rule line; realization via direct _m2a_wrap_rendered calls: \x01 splits prose (wrap on AND off), \x03 -> space on normal/opaque/wrap-off lines, \x02 -> '─' rule of width max(1,W-1) (wrap on at W=40, wrap off at fallback 150), \x02 mid-prose on its own line between before/after.
        Touched ONLY: constants block, md2ansi() input step, _m2a_wrap_rendered, + tests. Did not modify _md2ansi, _m2a_build_context, placeholder rewrite, style stack, any rule table, or add rules/producers. Full suite: 190 passed (175 + 15).

  * ## Ticket: Task: `<br>` and in-container `<hr>` — deferred line sentinels {#79}

        status: done
        created: 2026-06-24 01:12:42 UTC
        updated: 2026-06-24 02:04:15 UTC

    Spec §5.2, §5.3 (inline rule + container realization), §7. Depends on the sentinel
    ticket. Add to `_M2A_RULES_INLINE_RAW` (after `escape`, near the comment rule):
    `html_br` = `(?i: < br [ \t]* /? > )` → emit `\x01`; `html_hr_inline` =
    `(?i: < hr [ \t]* /? > )` → emit `\x02`. Realize in the opaque layout owners:
    - `_m2a_fmt_table`: in `cell_sublines`, split the rendered cell on `\x01` and `\x02`;
      `\x02` is a zero-width rule sub-line measured as `\n\n` (one extra line, no width
      demand), materialized as `─ × widths[i]` AFTER column widths are frozen.
    - `_m2a_fmt_list`: split item content on `\x01` (hard break, hang indent) and `\x02`
      (`─` at item-content width).
    - `_m2a_fmt_blockquote`: `\x01` → break (bar per line); `\x02` → `─ × (width-2)`.
    - `_m2a_fmt_heading`: `\x01` → `\n` (multi-line, color re-emitted per line by
      `_m2a_inject_color`, each line opaque); `\x02` → `─ × (line_width-1)` heading-color
      line. Done before `_m2a_opaque`.
    Add scan kinds `html_br -> ("br","br")`, `html_hr_inline -> ("hr","hr")`.

    Tests: `<br>` in prose; in a TABLE cell (multi-row, top-aligned, dividers); in a
    NESTED list item (hang indent preserved); case/self-close variants; in a heading
    (multi-line, each line still colored + opaque). `<hr>` in a table cell (`─` at column
    width, width measured before materialization); in a nested list item (`─` at item
    width, not full page); in a heading (colored rule line); mid-prose → full-width.

    * ## Comments {#79:comments}

      * Implemented <br>/<hr> deferred line sentinels. {#79:comment:85}

        PRODUCERS: added _MD_HTML_BR / _MD_HTML_HR_INLINE patterns + _m2a_fmt_br (->\x01) / _m2a_fmt_hr_inline (->\x02) handlers; two rules in _M2A_RULES_INLINE_RAW after html_comment; scan kinds html_br->(br,br), html_hr_inline->(hr,hr). Prose/mid-prose realization was already wired by #78's _m2a_wrap_rendered.
        CONTAINER REALIZATION (each materializes \x01/\x02 BEFORE _m2a_opaque, per the #78 INVARIANT):
        - Heading: \x01->\n, \x02-> '─'*(line_width-1) line, then inject_color+opaque (multi-line/colored-rule heading; each line stays colored & opaque).
        - Blockquote: \x01->\n (bar per line), \x02-> '─'*(wrap_width-2) line; before bar prefix+opaque.
        - List: \x01-> hard break with hang indent ('  '*level+'  '); \x02-> '─' at item-content width (wrap_width-len(hang)); only the bullet line keeps the bullet, all continuations hang.
        - Table: cell_sublines splits on \x01 (new sub-line) then \x02 (a single _M2A_RULE marker sub-line); render_row materializes a marker sub-line as '─'*widths[i] AFTER widths are frozen.
        TABLE RULE-SIZING (the hard part): the rule demands ZERO width during measurement. _col_actual treats a sub-line == _M2A_RULE as width 0, so a column is sized by real text only; the rule then fills the frozen width at render. Verified: 'a<hr>' in a column sized by a wider sibling row is NOT widened by the rule, and a <hr>-only cell collapses to the floor (1). The initial pre-split width estimate counts the literal sentinel chars, but _reconcile_column re-measures from the split sub-lines (shrink branch), so the final width is correct.
        EDGE CASES: combined <br>+<hr> in one cell stacks correctly with row dividers; <br> at cell edges yields blank top/bottom sub-lines; line_width=0 falls back to the page-width-based rule (mirrors _m2a_fmt_hr's 150 fallback); stress-tested 40 input/width combos (incl. <hr><br><hr> cells, multi-rule headings, list-in-quote) with no sentinel/replacement-char leaks and no hangs. Added an INVARIANT guard test asserting no \x00/\x01/\x02/\x03/U+FFFD survives in table/list/quote/heading output across lw in {0,40,80}.
        SPEC DEVIATION (flagged): inline <hr> (html_hr_inline) maps to ('hr','hr') exactly as the spec's §6 scan-API row dictates, so 'hr' is now reachable as BOTH a block (html_hr, standalone line, is_block=True) and inline (html_hr_inline, is_block=False) kind. This breaks the old M2A_SPANS_BLOCK.isdisjoint(M2A_SPANS_INLINE) invariant (they now overlap on exactly {'hr'}) and means a default block scan surfaces inline <hr> spans too (distinguished via span.is_block). Updated the two affected scan tests (partition + inline-kinds-contents) and renamed test_html_hr_with_trailing_text_is_not_block_rule -> ..._is_inline_rule_not_block (the inline rule now legitimately makes '<hr> x' a mid-prose full-width rule + trailing text, per §5.3). Did NOT touch _m2a_wrap_rendered. Suite: 205 passed (190 baseline + 15 new).
      * Addressed review: rule-adjacent empty-segment blank in cell+heading; removed dead or-[""], fixed commit message. {#79:comment:86}

  * ## Ticket: Task: HTML entities — `&name;` / `&#dec;` / `&#xHEX;` {#80}

        status: done
        created: 2026-06-24 01:12:42 UTC
        updated: 2026-06-24 02:15:47 UTC

    Spec §5.4, §7, §8. Depends on the sentinel + `<br>`/container tickets. Add
    `html_entity` to `_M2A_RULES_INLINE_RAW` (after `escape`), pattern
    `& (?: \# [0-9]+ | \# [xX] [0-9a-fA-F]+ | [a-zA-Z][a-zA-Z0-9]* ) ;` and handler
    `_m2a_fmt_entity`. Add `_M2A_HTML_ENTITIES` dict (~25 common names: amp lt gt quot
    apos nbsp copy reg trade mdash ndash hellip bull middot sect para deg times divide
    laquo raquo larr rarr uarr darr pound euro cent yen). Decoding rules, in order:
    null/surrogate(0xD800-0xDFFF)/out-of-range(>0x10FFFF) → `�`; LF(0x0A)/CR(0x0D) →
    `\x01`; U+00A0 (incl. named `nbsp`) → `\x03`; other control (n<0x20, DEL 0x7F, C1
    0x80-0x9F) → `�`; else `chr(n)`. Unknown named entity → return the match unchanged
    (literal). Add scan kind `html_entity -> ("entity","entity")`.

    Tests: inert content (no MD interaction) — `&#42;`→`*` not italic, `&#95;` not
    emphasis, `&#124;` stays in one table cell, `&#35;` at line start not a heading;
    `&amp;`→`&`, `&amp;amp;`→`&amp;`, hex `&#x41;`→`A`; unknown `&notreal;` literal;
    entity inside `` `code` `` literal; `&nbsp;` non-breaking then space. Premature-decode
    hazards (spec §8): `&#10;`/`&#13;` safe in a table cell and nested list item, `&#10;`
    in prose → `\n`, decoded newline adjacent to a raw input control char → `\n`+`�`,
    `&#1;`/`&#7;`/`&#127;`/`&#128;`/`&#0;`/`&#xD800;` → `�`.

    * ## Comments {#80:comments}

      * Implemented HTML entities (&name;/&#dec;/&#xHEX;) as an inline rule decoded during the inline pass. {#80:comment:87}

        Pieces (all in md2ansi_lib.py, ONLY the 4 allowed locations + tests):
        - _M2A_HTML_ENTITIES: dict of 30 named entities -> single Unicode char (nbsp stored as explicit   escape so it can't be tidied to a plain space).
        - _m2a_entity_char(cp): the SHARED codepoint-routing helper both the named and numeric paths funnel through, so they always agree. Ordered, mutually-exclusive cases per spec 5.4: (1) NUL / surrogate D800-DFFF / >10FFFF -> U+FFFD (out-of-range also guards chr() against ValueError); (2) LF/CR -> \x01 safe break sentinel; (3) U+00A0 -> \x03 nbsp sentinel; (4) other control (C0 <0x20, DEL 0x7F, C1 80-9F) -> U+FFFD; (5) else chr(cp).
        - _m2a_fmt_entity handler: numeric body (starts with '#') parses dec or x/X-hex then routes through the helper; known named routes ord(char) through the helper; unknown named returns m.group(0) UNCHANGED (WHATWG literal pass-through). recurse=None so &amp;amp; decodes once to &amp;.
        - _MD_HTML_ENTITY pattern: & (?P<*body> \#[0-9]+ | \#[xX][0-9a-fA-F]+ | [a-zA-Z][a-zA-Z0-9]* ) ; with REQUIRED trailing ';' so AT&T / bare & / &amp (no ;) never match.
        - Rule added to _M2A_RULES_INLINE_RAW after escape, grouped with the other html_* rules (code spans already precede and consume whole, so entities in inline/fenced code stay literal).
        - Scan kind html_entity -> ('entity','entity'); 'entity' auto-extends M2A_SPANS_INLINE.
        Timing correctness is automatic: every Markdown rule matched the RAW source where the entity was still &#...;, so a decoded * / | / # can't retro-trigger emphasis/table-split/heading, and table widths measure the expanded text. Premature-decode hazards covered: LF/CR entities route through the safe \x01 break (table box intact, nested-list hang indent preserved, prose -> \n); a decoded newline adjacent to a raw input control char gives \n + U+FFFD (sanitizer maps the raw \x07 -> U+FFFD before the entity decodes to \x01); &#1;/&#7;/&#127;/&#128;/&#0;/&#xD800; -> U+FFFD. TAB entity &#9; -> U+FFFD (no carve-out; only LF/CR/nbsp do), distinct from the input sanitizer's raw-tab exemption.
        TDD: wrote 32 failing entity tests first (24 red initially; 5 coincidentally green via existing literal pass-through; +3 added during verification), implemented to green. Also updated the one pre-existing pinned-set assertion test_span_inline_kinds_contents to include 'entity'. Suite: 240 passed (208 baseline + 32).

  * ## Ticket: Task: Sync main design doc with new rules {#81}

        status: open
        created: 2026-06-24 01:12:42 UTC
        updated: 2026-06-24 01:12:42 UTC

    Update `md2ansi_lib.design.md`: extend the §7.1 rule-order table with the new rules
    (`html_comment`, `html_br`, `html_hr`/`html_hr_inline`, `html_entity`) in their
    actual positions, and add a short behavior note for HTML tags/entities + the sentinel
    set, pointing to the spec for detail. Docs only — no code, no tests.
